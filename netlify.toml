[build]
  functions = "netlify/functions"
  publish = "."

[functions]
  # Deaktiviere esbuild-Bundling - nutze node_modules direkt
  node_bundler = "nft"
  included_files = ["node_modules/**"]

# Netlify installiert automatisch npm dependencies aus package.json
